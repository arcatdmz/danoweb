{
  "name": "denolop",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:arcatdmz/denolop.git",
  "author": "Jun Kato <i@junkato.jp>",
  "license": "MIT",
  "scripts": {
    "start": "deno --allow-env --allow-net --allow-read --allow-write server.ts",
    "dev": "yarn cross-env DENO_ENV=development yarn start",
    "build": "cd ../client && yarn build",
    "build-editor": "cd ../client && yarn build-pages"
  },
  "devDependencies": {
    "cross-env": "^5.2.0",
    "typescript": "^3.4.5",
    "typescript-deno-plugin": "^1.2.4"
  }
}
